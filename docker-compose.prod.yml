services:
  backend:
    environment:
      APP_ENV: production
      DEBUG: "false"
      LOG_LEVEL: INFO
      LOG_JSON: "true"
      WORKERS: ${WORKERS:-2}
    restart: always

  frontend:
    restart: always

  prometheus:
    restart: always

  grafana:
    restart: always
